@{
  Layout = "_Layout-Admin";
}
@model SillyString.Models.Machine

<h2>Editing machine <span class="highlighted">@Html.DisplayFor(model => model.Name)</span></h2>

@using (Html.BeginForm())
{
  @Html.HiddenFor(model => model.MachineId)
  <div class="form-row">
    @Html.LabelFor(model => model.Name)
    @Html.EditorFor(model => model.Name)
    <button class="green" type="submit">Save</button>
  </div>
}

<div class="button-area">
  <a href='@Url.Action("Details", "Machines", new { id = Model.MachineId })'>
    <button>Cancel</button>
  </a>
</div>